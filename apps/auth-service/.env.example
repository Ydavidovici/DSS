# .env.example
JWT_EXPIRES_IN=1h

PORT=4000

# Redis
REDIS_URL=redis://localhost:6379

# DB service (internal)
DB_SERVICE_URL=http://localhost:3002
DB_SERVICE_API_URL=http://db-service:4000/api/v1
DB_SERVICE_BASE_URL=http://db-service:4000
DB_SERVICE_AUDIENCE=db-service
AUTH_SERVICE_AZP=auth-service
# AUTH_PUBLIC_KEYS_DIR=./src/keys

# JWT / OIDC
JWT_ISSUER=https://auth.localhost:4000
JWT_AUDIENCE=dss-services
CURRENT_KID=auth_rsa
JWKS_PATH=./src/keys
RSA_MODULUS_BITS=3072

# Cookies / CORS
COOKIE_SAMESITE=lax
COOKIE_DOMAIN=
CORS_ORIGINS=
NODE_ENV=development

# Redirect allowlist bootstrap (comma-separated origins)
ALLOWED_REDIRECTS=https://app1.localhost:3000,https://app2.localhost:3000

# SMTP
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=